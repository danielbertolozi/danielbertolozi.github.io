# Initial commit

* Committing built TS files to be able to run this from github.io

# Commands

`start`: Run local server that mocks GH response, allows to test local files
`tsc`: Compiles TS
`webpack`: Runs webpack
`build`: Compiles TS and runs webpack
`test`: Run unit tests with mocha
